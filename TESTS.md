# Tests

## Testing Strategy
- **Unit Tests**: Coverage for `/src/engine` logic (spending calculation).
- **Integration Tests**: API endpoints (`/api/audit` and `/api/summary`).
- **E2E Tests**: Ensure the audit funnel completes successfully.
